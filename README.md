# Raphael Onuku

Personal research website for Raphael S. Onuku.

## Local preview

Open `dist/index.html` in a browser or serve `dist` with any static file server.

## GitHub Pages

The included workflow publishes the contents of `dist` whenever changes are pushed to `main`.
